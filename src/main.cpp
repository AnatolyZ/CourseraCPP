#include <iostream>

using namespace std;

int main() {
	int qn; // queries number
	cin >> qn;
	while (qn){
		string cmd;
		cin >> cmd;
		if (cmd == ""){

		}else if (cmd == " "){

		}
		--qn;
	}
	return 0;
}
